// import React, { createContext } from "react";

// export const StepContextProvider = createContext(null);

// export const StepContext = ({ children }: { children: any }) => {
//     // Any Logic Building
//     const website = "xzy.com";

//     return (
//         <StepContextProvider.Provider value={{ id: website }}>
//             {children}
//         </StepContextProvider.Provider>
//     );
// };
